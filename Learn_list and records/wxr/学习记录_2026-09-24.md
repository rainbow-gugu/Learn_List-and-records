# 学习记录 - 2026-09-24

## 今日学习状态

摸鱼ing~

### 检测情况
开始学习Finins-RAG项目，langchain和RAG核心的手写模块有优点
面试题：
什么时候选择手写模块（手搓agent），什么时候使用langchain
1.轻场合不堆砌重量工具，重场合不重复手写所有逻辑
2.langchain适合长链路推理
3.手搓，链路精简，不会被框架限制，全程可控（逻辑自己定义，出问题可以直接定位）



leetcode:
两数之和：
 class Solution:
    def TwoSum(self,nums,target):
        for i in range(len(nums)):
            res =target-num[i]
            if res in num[i+1:]:
                return [i,num[i+1:].index(res)+i+1]


**列表切片**：`nums[start:]` 截取从 start 到末尾的子列表
**列表方法**：`.index()` 获取元素的下标